export interface Battery {
  id: string;
  name: string;
  capacity: string;
  voltage: string;
  weight: string;
  dimensions: string;
  price: number;
  category: string;
  image: string;
}

export interface ContactForm {
  name: string;
  email: string;
  message: string;
}